import 'package:flutter/material.dart';

class ColorRounder extends StatelessWidget {
  const ColorRounder({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}