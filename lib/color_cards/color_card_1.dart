import 'package:flutter/material.dart';

class ColorCard1 extends StatelessWidget {
  const ColorCard1({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}